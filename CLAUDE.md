# agent-teams-v2 — Team Operations Guide

> Auto-generated by agent-teams-mcp-setup (2026-04-26).
> 这个文件让 team-lead 知识跨 context 压缩持久化。
> **重要**：项目根有相同内容的 `AGENTS.md`（codex worker 自动注入）和 `CLAUDE.md`（Claude Code lead 自动注入）。修改时两个都要改。

## Project

- 名字：`agent-teams-v2`
- Cwd：`E:\aigc内容整理\agent-teams-rs-team-mode`
- 目标：扩展现有 `agent-teams-mcp` 的 Web UI（`web/team-mode/index.html`）+ 继续后端开发
- Web UI 端口：`http://127.0.0.1:8787`
- 主 plan：`.plans/agent-teams-v2/task_plan.md`

## Team-Lead Control Plane

- team-lead = 主对话本身（Claude Code），不是 spawned worker
- team-lead 拥有用户对齐、scope 控制、任务拆解、phase 转换
- team-lead 维护项目全局事实：主 task_plan.md、decisions.md、CLAUDE.md/AGENTS.md
- team-lead 决策一个工作流改进是项目本地还是回写到 `agent-teams-mcp-setup` skill
- **不再 spawn 标准 subagent**：所有工作通过 worker。除非永久加新 worker

## Team Roster

| Name | Role | Adapter | Model | Cwd | 关键能力 |
|------|------|---------|-------|-----|---------|
| backend-dev | Rust backend (std-only HTTP server + team-mode MCP) | codex | default(GPT-5) | 项目根 | Rust 服务端代码 + TDD + Doc-Code Sync |
| frontend-dev | Web UI 前端 | codex | default | 项目根 | 扩展 `web/team-mode/index.html` + dashboard |
| researcher | 代码搜索 + Web 研究 | codex | default | 项目根 | 只读 — 摸现有架构、调研可视化方案 |
| e2e-tester | E2E 测试 | codex | default | 项目根 | Playwright 或 cargo test/curl 集成测（先自检 MCP 能力） |
| reviewer | 代码 review | codex | default | 项目根 | 只读 src/，写 review 报告 |
| custodian | 合规 + 文档治理 + check 脚本 | codex | default | 项目根 | docs/index.md、golden_rules.py、cleanup |

## Codex 项目级文件说明

**两份相同内容的根级 instructions**：
- `CLAUDE.md`（你正在读的）—— Claude Code（lead）启动时自动注入
- `AGENTS.md` —— codex CLI 启动时自动注入到所有 codex worker

修改一个必须同步另一个，否则 lead 和 worker 的协议会漂移。custodian 负责检查两者一致性。

## Task 派发协议（team-mode MCP）

### 双轨派发

team-mode MCP 没有跨 worker 共享 task list（codex/gemini 没 TaskList）。任务通过两条线下发：

1. **文件事实源**：`.plans/agent-teams-v2/<agent>/task_plan.md` —— lead 维护
2. **触发消息**：`mcp__team-mode__send_message(team="agent-teams-v2", text="@<agent> 你的新任务在 .plans/.../task-X/，请确认。")`

worker 收到 `@` 通知后读文件，按文件干活。

> **lead 私人 todo**：lead 自己用 `TaskCreate` 当本会话待办没问题（纯私用）。但**不能**拿 TaskCreate 当跨 worker 派发渠道（worker 看不到）。

### 大任务（特性、新模块）派发前 STOP AND CHECK

每条派单消息至少含 4 项（少任意一项加上再发）：

1. **Scope & 目标**：要做什么 + 验收标准
2. **文档提醒**："请建 task 子目录 `<prefix>-<task-name>/` 含 task_plan.md + findings.md + progress.md，并在自己根 findings.md 加索引"
3. **依赖**：依赖哪个 research / 任务，关键文件路径
4. **Review 期望**：完成后是否需要 review

任务子目录前缀：
- backend-dev / frontend-dev: `task-<name>/`
- researcher: `research-<topic>/`
- e2e-tester: `test-<scope>/`
- reviewer: `review-<target>/`
- custodian: `audit-<scope>/`

### 小任务（bug、配置）

直接消息描述。无需 task 子目录或 review。

## 通讯快查

| 动作 | 命令 |
|------|------|
| 派任务给单个 worker | `mcp__team-mode__send_message(team="agent-teams-v2", text="@<name> ...")` |
| 派多 worker | `text="@<name1> @<name2> ..."` |
| dev 找 reviewer | dev 在 reply 写 `@reviewer ...`（lead 通过 lead-observability 自动看到） |
| 看活的 worker | `mcp__team-mode__worker_list(team="agent-teams-v2")` |
| 复活死 worker | `mcp__team-mode__worker_add(team, name, on_existing="reuse")` |
| 软删 worker | `mcp__team-mode__worker_remove(team, name)` |
| inbox 兜底 | `mcp__team-mode__inbox_read(team="agent-teams-v2")` |

## 状态自检

| 看什么 | 怎么看 |
|--------|-------|
| Worker 列表 | `mcp__team-mode__worker_list(team="agent-teams-v2")` |
| 团队列表 | `mcp__team-mode__team_list()` |
| 快速扫 | 并行读各 worker 的 progress.md |
| 深入 | 读 worker findings.md（索引） → 进特定 task 子目录 |
| 方向校准 | Read `.plans/agent-teams-v2/task_plan.md` |
| 恢复项目 | Read `team-snapshot.md` → worker_add(on_existing=reuse) |

阅读顺序：**progress（在哪）→ findings（遇到什么）→ task_plan（目标是什么）**

## Docs 索引

| 文档 | 位置 | 维护人 |
|------|------|-------|
| Navigation Map | .plans/agent-teams-v2/docs/index.md | custodian |
| Architecture | .plans/agent-teams-v2/docs/architecture.md | researcher (P0)、devs |
| API Contracts | .plans/agent-teams-v2/docs/api-contracts.md | backend-dev（API 改必同步） |
| Invariants | .plans/agent-teams-v2/docs/invariants.md | team-lead, reviewer |

**Doc-Code Sync 规则**：代码改 API 或架构时，对应 docs/ 必须同任务更新。

## 自动化检查

| Check | 脚本 | Enforces |
|-------|------|---------|
| Golden Rules | scripts/golden_rules.py | 文件大小 / secret / TODO 残留 / 文档新鲜度 / invariant 覆盖 |
| Rust CI | scripts/run_ci.py | golden rules + cargo fmt --check + cargo clippy -D warnings + cargo test |

## Review Dimensions

| # | 维度 | Weight | STRONG 长这样 | WEAK 长这样 |
|---|------|--------|--------------|------------|
| RD-1 | 视觉与交互一致性 | high | UI 风格统一、状态反馈明确、loading/empty/error 三态都有 | 风格混乱、操作没反馈、报 raw error |
| RD-2 | 实时性 / 推送可靠性 | high | SSE/WS 不丢不重、断线自动重连、UI 状态实时跟得上 | 状态需手动刷新、连接断后无恢复 |
| RD-3 | Rust 代码质量 | high | 错误用 `Result + thiserror`、并发安全、`cargo clippy` 零告警、模块边界清晰 | unwrap 满天飞、有 race、clippy 告警未修 |
| RD-4 | 可观测性 + API 设计 | medium | 关键操作发结构化事件 / log；REST 路径清晰、版本化、向后兼容 | 失败无日志线索；API 路径混乱、字段命名乱 |

> 任一维度 WEAK → reviewer verdict 不能 [OK]
> 标准 checklist（安全/质量/性能/doc-sync）总是叠加

## Harness Checklist

team-lead 在 phase 边界 review（不是每个任务）：

### Operational Health（每 phase 边界）

- **Docs harness**：CLAUDE.md/AGENTS.md + 主 task_plan.md 还准吗？陈旧 → 更
- **Observability harness**：Grep progress.md `error|fail` —— 失败有详细记录吗？
- **Invariant harness**：Known Pitfalls 有该升 reviewer checklist 或自动测试吗？
- **Replay harness**：本 phase 有可复用模式（搜索策略、架构模板）吗 → 标 [TEAM-PROTOCOL]

### Assumption Audit（模型升级或复盘）

每个 harness 组件编码了对模型局限的假设。模型升级 / 项目复盘时审：组件上 phase 触发 <2 次 **且** 移除不会有质量下降 → 候选移除/简化。

## Known Pitfalls

> 重复失败模式 append。每条防止再发生。

(初始空)

## Style Decisions

> 用户 taste 偏好。同模式 3+ 次 → custodian 编码到 golden_rules.py。

| # | 决策 | 来源 | 状态 |
|---|------|------|------|

(初始空)

## Key Protocols

| 协议 | 触发 | 动作 |
|------|------|------|
| 需求对齐 | 团队建好、dev 开始前 | researcher 探代码库（T1），team-lead 与用户对齐 |
| Plan stress-test | 架构最终化前 | 派 researcher："stress-test 这个 plan，走每个决策分支" |
| 3-Strike escalation | worker 报 3 次失败 | 读其 progress.md，给新方向或重派 |
| Code review | 大特性/模块完成 | dev 在 findings.md 写变更摘要，reply 里 @reviewer |
| Phase 推进 | phase 完成 | 等 reviewer [OK]/[WARN] 才推 |
| Context overflow | worker 报 context 长 | 进度已存盘，恢复或 spawn 继任 |
| CI gate | 任何代码改动 | 跑 `python scripts/run_ci.py`，全 PASS 才送审 |
| Guardrail capture | 3-Strike 解决 / reviewer [BLOCK] 修完 | 会复发吗？是 → Known Pitfalls；通用 → [TEAM-PROTOCOL] |
| Custodian audit | 2-3 dev 任务完后 / phase 边界 | lead 触发 custodian 合规扫描 |
| Pattern → Automation | reviewer 标 [AUTOMATE] 重复模式 | lead 派 custodian → 写 check → 加 CI |
| Taste capture | 用户表 style/命名/结构偏好 | 记 Style Decisions。3+ 次 → 派 custodian 编码 |
| Template sync | 发现可持久工作流改进 | 先改 agent-teams-mcp-setup 源文件 |
| 团队重建时机 | 模板改动影响 worker 行为 | 优先 phase 边界 |

## 文件结构

```
.plans/agent-teams-v2/
  task_plan.md          -- 主 plan（精简导航图）
  team-snapshot.md      -- onboarding prompt 缓存（恢复用）
  findings.md           -- 团队级 findings
  progress.md           -- 工作日志
  decisions.md          -- 架构决策日志（ADR）
  docs/                 -- 项目知识库
    index.md            -- 导航图（custodian 维护）
    architecture.md
    api-contracts.md
    invariants.md
  archive/
  <agent-name>/
    task_plan.md
    findings.md         -- INDEX
    progress.md
    <prefix>-<task>/
      task_plan.md / findings.md / progress.md
```
