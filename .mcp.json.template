{
  "_comment_template": "This is the TEMPLATE. scripts/setup.sh / setup.ps1 writes it to `.mcp.json` (gitignored). Do not edit `.mcp.json` directly — re-run setup after any path change.",
  "_comment_devs": "Team Mode uses a durable localhost Streamable HTTP MCP service. Start it with scripts/team-mode-service.ps1 start, then fully restart Claude Code.",
  "mcpServers": {
    "team-mode": {
      "type": "http",
      "url": "http://127.0.0.1:8786/mcp",
      "headersHelper": "node scripts/mcp-http-headers.js"
    }
  }
}
