{
  "_comment": "Default points at the release build with project-relative path. Build with: cargo build --release --bin team_mode_mcp --bin team_mode_daemon. ${EXE_EXT:-.exe} = '.exe' on Windows; POSIX users: export EXE_EXT=\"\" before starting Claude Code. For dev iteration, locally override to target/debug — but DO NOT commit that.",
  "mcpServers": {
    "team-mode": {
      "command": "${CLAUDE_PROJECT_DIR}/target/release/team_mode_mcp${EXE_EXT:-.exe}",
      "args": [],
      "env": {
        "RUST_LOG": "info"
      }
    }
  }
}
